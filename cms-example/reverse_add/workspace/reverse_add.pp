
unit reverse_add;
interface

{
  Automatically converted by H2Pas 1.0.0 from reverse_add.h
  The following command line parameters were used:
    reverse_add.h
}

{$IFDEF FPC}
{$PACKRECORDS C}
{$ENDIF}


{$ifndef _REVERSE_ADD_H}
{$define _REVERSE_ADD_H}  

  procedure reverseAdd(x:longint);

  procedure reportAnswer(a:longint; b:longint);

{$endif}

implementation

  procedure reverseAdd(x:longint);
  begin
    { You must implement this function }
  end;
  procedure reportAnswer(a:longint; b:longint);
  begin
    { You must implement this function }
  end;

end.
